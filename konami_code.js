const code = [38, 38, 40, 40, 37, 39, 37, 39, 66, 65]

function init() {
  // keep track of index for key inputs outside of event handler
let index=0
console.log("Hello")
  // Write your JavaScript code inside the init() function
    const key = parseInt(e.detail || e.which)
  }
